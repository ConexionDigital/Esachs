@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


<ul class="navbar-nav ml-auto">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Reportes" asp-action="Index">Reporte</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Adminfuncionarios" asp-action="Index">Administración de funcionarios</a>
        </li>
        @*<li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Hola, @UserManager.GetUserName(User)
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li class="dropdown-item">
                    <a class="nav-link" asp-controller="Usuarios" asp-action="CambiarPassword">Cambiar contraseña</a>
                </li>

                <li class="dropdown-item">
                    <form id="logoutForm" class="form-inline" asp-controller="Usuarios" asp-action="Logout">
                        <button type="submit" class="btn nav-link">Salir</button>
                    </form>
                </li>
            </ul>
    </li>*@
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Ingreso" asp-action="Index">Acceso Usuarios</a>
        </li>
    }

</ul>